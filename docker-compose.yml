services:
  postgres:
    image: postgres:15-alpine
    container_name: manejotareas-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: manejotareas
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d manejotareas"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
